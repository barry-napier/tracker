# Tracker

Desktop app. Bare Electron scaffold — no functionality yet.

```bash
npm install
npm start
```

- `src/main.mjs` — main process (ESM)
- `src/preload.cjs` — context-isolated preload bridge
- `src/renderer/` — static renderer
