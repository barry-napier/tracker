# Dogfood report — E2E-1: Machine-readable widget list

> Verdict: **READY**
> Frozen SHA: `b7ac1b9a581cf4ec90dd033b35629e7eefa1d62c` · Base: `main` · Scenarios: 3 green / 3 total
> Fixes: 0 · Paper cuts: 0 sharp / 0 total

## Matrix

| # | Journey (past the endpoint) | Kind | Functional | Experiential | Evidence | Fix |
|---|---|---|---|---|---|---|
| S1 | Consumer parses widgets.json and the resulting string array matches the non-empty lines of widget.txt element-for-element, in order (compared against widget.txt's actual bytes) | http | ✅ pass | — | kb/evidence/S1-ac1.txt | — |
| S2 | New contributor reads README.md and learns what widgets.json contains (JSON array of widget name strings), that it derives from widget.txt's non-empty lines in order, and how to regenerate it (by hand — no generator script) | http | ✅ pass | — | kb/evidence/S2-ac2.txt | — |
| S3 | Strict JSON parser (no leniency) consumes widgets.json with no error and every entry is a string — downstream tooling can rely on the type contract | http | ✅ pass | — | kb/evidence/S1-ac1.txt | — |

**Cut from the matrix** (cap is 12, ranked by risk): nothing

Note on "Kind": no preview environment is configured for this repo and the diff is pure static data + docs — there is no server to curl or page to drive. The observable surface is the artifact files themselves, so each scenario was walked at the filesystem/CLI level (strict parse, byte comparison, README read). "http" is the closest schema value; no browser journeys exist for this change.

## Paper cuts

No persona for this repo → experiential judge skipped.

No functional paper cuts found. The README even documents the regeneration story (manual, no generator), which pre-empts the most likely future confusion.

## Decisions for a human

<!-- Empty section = no open questions. -->

## Instruments

- Suite: `./checks/ac-1.sh && ./checks/ac-2.sh` → both PASS (exit 0) at `b7ac1b9a581cf4ec90dd033b35629e7eefa1d62c`
- Console/network harvest: n/a — no browser surface; clean
- Preview: none configured for this repo; the diff has no runnable surface (static JSON + README), so nothing was blocked by its absence
- Not covered: drift over time — if widget.txt changes, widgets.json must be regenerated by hand and no CI check enforces the match beyond checks/ac-1.sh. Acceptable for this ticket's scope; the README states the manual process explicitly.
