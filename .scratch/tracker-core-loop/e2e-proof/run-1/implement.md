# E2E-1 Implementation: Machine-readable widget list

## What was done

1. **Created `widgets.json`** at the repo root. Generated programmatically (per
   kb/plan.md) by reading `widget.txt` and keeping non-empty lines in file
   order — not hand-typed. Result: `["the widget"]` with a trailing newline.

2. **Edited `README.md`** — added a `## widgets.json` section under the existing
   heading stating that the file is a JSON array of widget names (strings),
   derived from the non-empty lines of `widget.txt` in the same order, and that
   it must be regenerated by hand when `widget.txt` changes (no generator
   script exists, per the plan's non-goals).

## Verification

Ran both check scripts from `checks/manifest.json`:

- `checks/ac-1.sh` (AC-1): PASS — widgets.json exists, parses as a JSON array
  of strings, entries match non-empty lines of widget.txt in order.
- `checks/ac-2.sh` (AC-2): PASS — README documents widgets.json contents and
  derivation from widget.txt.

## Notes / decisions

- `widget.txt` untouched.
- No generator script, CI, or package manifest added — out of scope per
  kb/research.md and kb/plan.md.
